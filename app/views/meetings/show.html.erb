<%= form_for [current_user, @meeting], :html => { :multipart => true } do |f| %>

Meeting of <%= @user.languages_without_learning %> organized by <%= @user.name %> in <%= @meeting.place_meeting %> at <%= @meeting.date %>

	<%= f.text_field :participants, :value => current_user.name %>
	<br>
	<%= f.text_field :participants, :value => current_user.nick %>
	<br>
	<%= f.submit 'Assist!', class: "btn-submit" %>
<% end %>
